export enum UserStatus {
  Active = "ACTIVE",
  Suspended = "SUSPENDED",
}

export enum ProductAttributesEnum {
  Storage = "storage",
  Color = "color",
}

export enum ContactType {
  Partner = "Partner",
  Complaint = "Complaint",
}

export enum CategoryType {
  LAPTOP = "laptop",
  ACCESSORY = "accessory",
}
